	// We will initialize struct address with the right values and test to confirm it works
	void	init_servers(struct sockaddr_in& sockAddr, std::string& listen) {
		std::string		ip;
		std::string		port;

		listen.substr(0, )

		int pos;

		if ( (pos = listen.find(":")) != std::string::npos ) {

		}
		// 	if (  )

		std::cout << "pos test: " << listen[pos] << std::endl;

		// listen.substr()

		(void)sockAddr;
		// (void)_listen;
	}